{% extends 'base.html.twig' %}

{% block title %}Semestre{% endblock %}

{% block body %}
    <h1>Semestre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>NumeroSemestre</th>
                <td>{{ semestre.numeroSemestre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('semestre_edit', {'id': semestre.id}) }}">Modifier</a>

    {{ include('semestre/_delete_form.html.twig') }}
{% endblock %}
