{% extends 'base.html.twig' %}

{% block title %}Parcours{% endblock %}

{% block body %}
    <h1>Parcours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Niveau</th>
                <td>{{ parcour.niveau }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('parcours_edit', {'id': parcour.id}) }}">Modifier</a>

    {{ include('parcours/_delete_form.html.twig') }}
{% endblock %}
